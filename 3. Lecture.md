## Sets and Sorting

A set interface, is an object that you can keep on adding things to it

Eg, a set that represents all the students in a class

Container | build(A), len() | given an iterable A, build a sequence from items A return the number of stored items
Return the stored item with key k find(k)
Insert or delete - Dynamic operation
Order - find smallest key, largest key, find next, find previous

Implementing a set

1. Store an array of objects: Unordered array

Searching the array linearly takes O(n) time
So maybe sorting the list once
the search would be less costly

Building a set steps
- Reserve n slots in memory
- Then copy items into the set

2. What if we kept the items in our set organized by a certain key
Say from smallest to largest
That would mean searching is quicker later

Array
Container: build(A) - n
Static: find(k) - n
Dynamic: insert(k) and delete(k) - n
Order: find_min(), find_max() - n
find_prev(k), find_next(k) - n

Sorted Array
Container: build(A) - n log n
Static: find(k) - log n
Dynamic: insert(k) and delete(k) - n
Order: find_min(), find_max() - 1
find_prev(k), find_next(k) - log n

#### How do we get a sorted array?
How can I take a disorganized set, and put it in a sorted order
so as to make it easier to search later

1 Billion Operations would take 9, log 1Billion = 9

#### Sorting vocabulary
##### Destructive
Overwrites the input array
##### In place
Not only are they destructive, but also Uses O(1) extra space

====
Input : Array of n numbers/keys A
Output: Sorted array B

===
First way

Permutation sort
List every possible permutation and check which of them is in the right order

permutation_sort(A): 
1. Enumerate perms: Omega (n!)
2. Check if perm is sorted O(n)
-- for i=1 to n-1
--- is B[i] <= B[i+1]

The algorithm takes Omega (n! . n)
- The worst algorith seen

===
Selection sort: O(n)
8 2 3 9 3
Sort the above

Algo
Find the biggest number, and stick it to the end
Take 9 and swap it with 3
Find next biggest number
Take 8 and swap it with the 3

==
1. Find the biggest with index <= n
2. Swap
3. Sort from i to i-1

==
function prefix_max(A, i):
    if (i > 0)
        j = prefix_max(A, i-1)
        if A[i] < A[j]:
            return j
    return i

so everything beyond i is alreay sorted

function selection_sort (A, i = None):
    if i is None: i = len(A) - 1
    if i>0:
        j = prefix_max(A, i)
        A[i], A[j] = A[j], A[i]
        selection_sort(A, i-1)

Insertion sort O (n)
Opposite of Selection sort, takes 

===
Merge sort: O(log n)
7, 1, 5, 6, 2, 4, 9, 3

Take every pair, exchange them
Expand to pairs of 4

Merge sort computes the middle of the array : c
which is a + b + 1 // 2
sort from a to c
sort from c to b
merge

Implement merge
Think of two fingers, the upper finger i and lower finger j
from n of the two arrays

merge (L, R, i, j, a, b):
    if a < b:
        if ( j<=0) or 

Not clicking, lets try this another day, hehehe - Merge sort